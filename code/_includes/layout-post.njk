<!DOCTYPE html>
<html lang="es-MX">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}}</title>
  <meta name="description" content="{{description}}">
  <link rel="icon" href="/img/RockLogo.jpg">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Jaro:opsz@6..72&family=New+Rocker&family=Nunito:ital,wght@0,200..1000;1,200..1000&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/estilos.css">
</head>
<body>
  {% include "header-post.njk" %}
  <main>
    {{content | safe}}
    <hr>
    <img class="band-image" src="{{imagen}}" alt="{{title}}">
    <hr>
    <h2 class="jaro-subtitle">Sus más grandes hits</h2>
    <br>
    {{song1 | safe}}
    <br>
    {{song2 | safe}}
    <br>
    {{song3 | safe}}
    <hr>
  </main>
  <aside>
    <h2 class="jaro-title">Más bandas</h2>
    <h3 class="jaro-subtitle">Bandas de Rock</h3>
    <ul>
      {% for article in collections.rock %}
      <li><a href="{{ article.url }}">{{ article.data.title }}</a></li>
      {% endfor %}
    </ul>
    <h3 class="jaro-subtitle">Bandas de Metal</h3>
    <ul>
      {% for article in collections.metal %}
      <li><a href="{{ article.url }}">{{ article.data.title }}</a></li>
      {% endfor %}
    </ul>
  </aside>
  {% include "footer-post.njk" %}
  <script src ="/js/main.js" type="module"> </script>
</body>
</html>